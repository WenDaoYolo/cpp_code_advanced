
不同类型的数据其具体的存储规则不一样，但不管怎样最终都会转换成二进制整型的形式来存储
```
整型数：无符号数->机器数存储，有符号数->原反补规则 ->二进制
浮点数：IEEE754标准 ->二进制
```

字节序的概念：大端存储与小端存储，分别使用大端字节序和小端字节序存储数据
```
数据以什么形式进行存储，就以什么形式取出还原

大端字节序：数据高位存放在低地址处，数据低位存放在高地址处
小端字节序：数据高位存储在高地址处，数据低位存放在低地址处
```

大小端存储的意义
```
解决多字节数据存放的顺序问题，内存单元的大小为一个字节，而对于占用空间
大于一字节的数据，则需要多个内存单元，于是就涉及到他们之间的顺序问题

例如int4个字节，doule8个字节
对于位数大于8的机器例如32位，64位则必定得解决多字节数据存放的顺序问题
```

当前环境(win11，vs2022)采用小端存储
![[小端存储.png]]
![[小端存储2.png]]

---

整型数的存储
```
无符号数：无符号位，即只有正数，以二进制机器数的形式存储运算和处理
有符号数：最高位为符号位，0正1负，以二进制补码的形式存储运算和处理
```

整型数的范围及其定义
```
例如int，short，long,long long，char(以ascll码的形式存储，所以本质也是整数类)等,其取值范围定义在limits.h中
```

原、反、补码：专门为有符号整型数设计的规则
```
正数的原反补：一致，均为二进制原码
负数的原反补：

原码：原二进制数,即机器数，又称为真值
反码：原码符号位不变，其他位取反
补码：反码+1，反推可以求其原码，补码的补码也可以求出其原码

注意，在运算中只是原码反码的转换时符号位不变其他取反，最终符号位都是要参与运算的
采用补码的其他原因：可以将减法转化为加法(-a->+(-a))，方便计算机的逻辑实现，简化了设计路线
```

原码中存在+0和-0，但是对于补码其形式是唯一的(全0)，这也是为什么计算机内部采用补码的原因之一
```
分析：+0补码全0原码也为0，-0运算后，高位溢出数据丢弃最后原码结果也为全0
```

所以对于最高位为1其余为0的补码，其原码不再解析为-0，而被解析为-2^(N-1)，详见进阶中的数据存储
```
例如 补码：10000000 -> 转为原码：1(溢出)00000000，不再解析为-0
直接解析为-128，所以也解释了为什么8位二进制补码表示的数其范围为-128到127
```
---

浮点数的存储
```
IEEE(电气和电子工程协会)制定了754标准(c语言遵循这个标准，其他语言未知)，规定了浮点数的表示，编码与解码
浮点数的表现形式、计算机中的,编码、解码有各自的转换规则，其中编码与解码就是互逆的过程
```

浮点数的范围及其定义
```
例如float，double，long double(c99)，3.14123，3.14E7(科学计数法3.14x10的7次方)等，取值范围定义在float.h中
```

浮点数的表示方式
```
任何一个二进制浮点数V都可以表示为：(-1)^S * M * 2^E
(-1)^S表示符号位:S为0则V为正数,S为1则V为负数
M表示有效数字>=1且<2                             (类似于10进制中的科学计数法，只不过这里是2进制的科学计数法)
2^E,其中E表示指数位

例如10进制单精度浮点数5.5：
转2进制补码(整数和小数分别转):101.1
101.1=1.011*2^2
754标准下的表现形式为：(-1)^0 * 1.011 * 2^2

其中：
正数则符号位为(-1)^0,S=0
有效数字1.011,M=1.011
指数部分2^2，E=2
```
---
浮点数在计算机中的编码(存储)方式

对于float单精度(4个字节32位)的存储规定：对于最高位为符号位S，对于后8位为指数位E，对于剩下的23位为有效数字M
![[float单精度的存储规则.png]]
```
S：正数0，负数1
M：固定格式1.xxxxx，由于小数点前面固定为1,计算机在处理时会默认前面为1不用专门存储,所以23位全用于小数部分的存储
对于位数不够的地方用0补齐
E：规定为8位无符号整数，其取值范围为0-255，而科学计数法中指数可能出现负数，所以存储的E需要通过加上一个中间数来修正，float的中间数为127
修正数为2^(N-1)-1,其中N为总的二进制位数
```

对于double双精度(8个字节64位)的存储规定：对于最高位为符号位S，对于后11位为指数位E，对于剩下的52位为有效数字M
![[double双精度的存储.png]]
```
S：同理正数0，负数1
M：同理52位全用于小数部分的存储,对于位数不够的地方用0补齐
E：规定为11位无符号整数，其取值范围为0-2^11-1，而科学计数法中指数可能出现负数，所以存储的E需要通过加上一个中间数来修正，double的中间数为1023
2^(N-1)-1
```
---
浮点数在计算机中的解码(取出)方式：编码的逆向过程

1.E不全为0或者不全为1(普通常规情况)
```
E-中间数
M前面加上整数1(1.)
S不变
还原并转为常规的十进制浮点数
```

2.E为全0
```
表示为无穷接近于0的一个数(+无穷小或-无穷小,具体正负取决于符号)

中间数很大，E若为全0，则0-中间数得到的为-中间数，而2^(-中间数)是一个非常小的数，极限接近于0
E为全0时，M会直接忽略整数部分前面不会添加1，直接以小数部分0.xxxx作为真实的有效数字(这样做是为了表示无限接近于0的数,+0或-0)
```

3.E为全1
```
表示为+无穷大或-无穷大(具体正负取决于符号)

原因分析：(同理,得到指数v，而2^v是一个非常大的数字)
参考对比：2的50次方约等于1000万亿,再乘上有效数字和符号位实际也是一个无穷大的数字
```

编码与解码中的E
```
编码存储E+中间数修正，解码取出E-中间数还原
```
---
计算机内部所有的数据都以二进制整型的形式来运算和处理，具有一定的运算规则


隐式类型转换：cpu和编译器自动进行的数据类型转换，包括整型提升、数据截断、算术转换
```
cpu的特性：cpu内的运算操作的都是整型数,所以在运算时若数据大小低于整型的大小,为了保证精确度则会先对其
整型提升后再送入cpu进行处理，运算完的结果若要存储到一个低于整型大小的变量之中则会对其进行数据截断截断
```

整型提升
```
进行整型操作或运算时,若运算对象的类型大小低于整型时会进行整型提升，高位补数提升至整型大小
整型提升高位的补充数是由提升对象的类型和符号(signed,unsigned)确定的，与其他无关
有符号数高位补原符号位，无符号数高位统一补0

对于默认char，c语言没有规定是signed还是unsigned，取决于编译器(大部分是signed)
对于默认整型，例如int b,short s等，c语言规定默认是signed
```

数据截断
```
把数据存储到变量中，当数据的类型大小大于变量的类型大小，则会从低位到高位进行截取，再存储到变量之中
注意，浮点数的数据截断与二进制位无关，而是直接对值进行处理(小数部分直接丢弃)
```

算术转换
```
又称寻常算术转换，或者向上转型(为了区分c++中的向上转型，建议就使用算术转换的术语)
也是隐式的，当操作符操作的几个对象都是不同类型的数据时，就会进行算术转换

转换的规则：将操作数向其中精度更高的类型或者更大数的类型进行转换(即占用字节更大的类型，即向上转型)，而所得的结果也是这个类型
```
---
例题：为了方便计算，过程可以忽略大小端的存储顺序，但涉及到高低位取值时要结合编译器采用的字节序


整型提升与数据截断
```
int test1=3.1233;                    //双精度浮点型为8个字节，整型为4个字节，则会对数据进行截断再存储到test1中
printf("%d\n",test1);					 
char test2=3;                        //进行数据截断后再存储               
3补码：000...0011
数据截断后：00000011

char test3=127;
127补码：000...01111111
数据截断后：01111111

char test4=test2+test3;              //进行操作符运算，则分别对test2，test3整型提升，运算后又将结果进行数据截断存储到test4中
test2:00000011
整型提升:000...00000011               //高位补符号位0
test3:01111111
整型提升:000...01111111
...
test4:10000010

printf("%d\n",test4);                //以整型的格式进行输出，则再次进行整型提升
整型提升:111...10000010               //高位补符号位-1
原码:100...01111110                  //所以结果为-126

char k='a';
printf("%d\n",sizeof(-k))            //操作符运算，则k进行整型提升，此时sizeof操作的则是整型，所以结果为4
printf("%d\n",sizeof(!k))            //同理
```

算术转换
```
int a=3;
double b=3.14;
a+b;                                 //则会进行算术转换，将a转为精度更高的double类型，其a+b值的类型也为double
                                     //通过监视窗口可以发现，a+b整体结果也为double类型
```

数据截断的注意事项：数据存储决定是否会进行数据截断
```
int a=3.14              //将3.14赋值给a，这里进行了存储，会对3.14进行截断再存入a
float b=6.33;
int c=a+b;              //将a+b的值赋值给c，这里进行了存储，会对结果进行截断再存入c
printf("c:%d\n",a);
printf("a+b:%d\n",a+b); //这里没有进行存储而是直接以整型输出，所以不会截断，结果是一个很大的整数(float被解析为int)
```

1.以下程序输出结果是什么?
```
    signed char a=-1;              
    unsigned char b=-1;            
    char c=-1;
    printf("%d,%d,%d\n",a,b,c);
```

```
    -1原码：10000000 00000000 00000000 00000001
      反码：11111111 11111111 11111111 11111110
      补码：11111111 11111111 11111111 11111111
    截断后：11111111

有符号char：11111111
  整型提升：11111111 11111111 11111111 11111111
有符号整型：10000000 00000000 00000000 000000001 = -1

无符号char：11111111
  整型提升：00000000 00000000 00000000 11111111
有符号整型：00000000 00000000 00000000 11111111 = 255

char 默认为有符号，所以 = -1

最终结果:-1,255,-1
```

2.以下程序输出结果是什么?
```
char a = -128;
printf("%u\n",a);
```

```
-128原码：10000000 00000000 00000000 10000000
    反码：11111111 11111111 11111111 01111111
    补码：11111111 11111111 11111111 10000000
  截断后：10000000

有符号char: 10000000
  整型提升: 11111111 11111111 11111111 10000000
无符号整型：11111111 11111111 11111111 10000000 = 一个非常大的正整数

最终结果：一个非常大的正整数
```
![[默认char整型提升.png]]

3.以下程序输出结果是什么?
```
char a=128;
printf("%d,%u\n",a,a);  
```

```
        128原码：00000000 00000000 00000000 10000000
           反码：00000000 00000000 00000000 10000000
           补码：00000000 00000000 00000000 10000000
         截断后：10000000

       整型提升：11111111 11111111 11111111 10000000
      有符号整型：10000000 00000000 00000000 10000000 = -128
      无符号整型：11111111 11111111 11111111 10000000 = 一个很大的正整数

       最终结果:-128，一个很大的正整数
```

4.以下程序输出结果是什么?
```
int a = -20;
unsigned b = 10;
printf("%d\n",a+b);
```

```
    -20原码：10000000 00000000 00000000 00010100
       反码：11111111 11111111 11111111 11101011
       补码：11111111 11111111 11111111 11101100
     10原码：00000000 00000000 00000000 00001010
       反码：00000000 00000000 00000000 00001010
       补码：00000000 00000000 00000000 00001010
       a+b：11111111 11111111 11111111 11110110  
  有符号整数：10000000 00000000 00000000 00001010 = -10
    最终结果：-10
```

5.以下程序输出结果是什么?
```
unsigned int i;
for (i = 9; i >= 0; i--) {
	printf("%u ",i);
}
```

```
unsigned，无符号整数，其值恒>=0
而循环的结束条件>=0,所以陷入死循环
```

6.以下程序输出结果是什么?
```
char a[1000];
int i;
for (i = 0; i < 1000; i++) {
	a[i] = -1 - i;
}
printf("%d\n", strlen(a));
```

```
    有符号char的取值范围：-128 到 127 共256个数
    正整数范围：   0 到 127
    负整数范围：-128 到 -1
    char只有8位,超出范围会数据截断然后重新进入这个范围
    
    -1-i:-1,-2...-127;-128,-129...-256;...
    a[i]:-1,-2...-127;-128, 127...  0 ;...
    
    -1补码：   11111111 11111111 11111111 11111111 -> 截断后：11111111 = -1
    -127补码： 11111111 11111111 11111111 10000001 -> 截断后：10000001 = -127
    -128补码： 11111111 11111111 11111111 10000000 -> 截断后：10000000 = -128
    -129补码： 11111111 11111111 11111111 01111111 -> 截断后：11111111 = 127
    -256补码： 11111111 11111111 11111111 00000000 -> 截断后：00000000 = 0

	一直在-1到-128，127到0之间循环
    strlen遇到\0(0,不包括)停止,所以-1到-256，共256-1=255个有效数据
    最终结果：255
```
![[计算机中8位二进制的实际取值范围.png]]
有符号8位数截断快捷计算法：超出范围的数
```
正数：-256
负数：+256
无符号(同样适用，其本质就是正数)：-256
```

7.以下程序输出结果是什么?
```
unsigned char a=0;
int main(){
	for(a=0;a<=255;a++)
		printf("hello word!\n");
	return 0;
}    
```

```
unsigned char 最大值11111111 = 255，其值恒小于0(超出范围会数据截断)
循环的条件<=255,所以会陷入死循环
```

8.
![[大小端存储面试题.png]]
```
int main() {
	int test01 = 1;                                  //0x00000001;
	if (*((char*)&test01) == 0)                      //通过char*类型的指针只访问第一个低地址的空间
		printf("当前机器采用大端字节序\n");
	else
		printf("当前机器采用小端字节序\n");
	return 0;
}
```

9.十进制单精度浮点数0.5的编码是什么
```
二进制：0.1
IEEE754表示：(-1)^0 * 1.0 * 2^(-1)

SME编码:
S：0
M：0000...(补齐23位)
E:-1+127=126

所以计算机中存储为:
0 0111111 000000000000000000000000
16进制:3F 00  00 00

通过内存窗口查看存储的值(小端存储)
```
![[浮点型数据的存储2.png]]

10.十进制单精度浮点数5.5的编码是什么
```
二进制:101.1
IEEE754表示:(-1)^0 * 1.011 * 2^2

SME编码:
S:0
M:011000...(补齐23位)
E:2+127=129

所以计算机中存储为:
0 10000001 01100000000000000000000
16进制:40 B0 00 00 

通过内存窗口查看存储的值(小端存储)
```
![[浮点型数据的存储3.png]]

11.以下程序输出什么结果?
```
int main() {
	int n = 9;
	float* pFloat = (float*)&n;
	printf("%d\n",n);
	printf("%f\n", *pFloat);
	*pFloat = 9.0;
	printf("%d\n", n);
	printf("%f\n", *pFloat);
	return 0;
}
```
输出结果：
![[浮点型数据的存储.png]]
```
以整型格式操作整型数，以浮点型格式操作浮点数，各自都没有问题，但以整型格式操作浮点数或反之则就出现了问题
所以：整数和浮点数的存储方式不一样
```
分析：
```
1.整数9，以整数方式输出则结果为9

2.整数9以浮点数的方式取出：
整数的存储:00000000 00000000 00000000 00001001
浮点数的视角：0 00000000 00000000000000000001001
S=0 E=0-127=-127 M=000...1001
解码为:(-1)^0 * 0.000...1001 * 2^(-127)=0.0000...xxx...
数据非常的小，而float精度只保留6位，所以后面的位都被舍弃结果则为0.000000

3.浮点数9.0以整数方式取出：
二进制补码:1001.0
754表现形式:(-1)^0 * 1.001 * 2^3
S=0，M=00100...(补齐23位)，E=3+127=130
浮点数的存储:0 10000010 00100000000000000000000
整数的视角:01000001 00010000 00000000 00000000B=1,091,567,616D

4.浮点数9.0，以浮点数方式输出则结果为9.000000
```










